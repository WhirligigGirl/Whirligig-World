//Hydrogen is found on the following planets in the following conditions:
//Kaywell		Exosphere
//Limnel		Exosphere
//Shol			Atmo, Exo
//Tyepolbynar	Atmo, Exo
//Jifgif		Exo, trace
//Imterril		Atm trc, Exo
//Tannor		Atm trc, Exo
//Mesbin		Atmo, Exo
//Statmun		Exo, trace
//Kerbmun		Exosphere
//Derbin		Atmo, Exo
//Valyr			Atmo, Exo
//Reander		Atmo, Exo
//Lito			Atmosphere
//Totooa		Atmosphere
//Fophie		Exo, trace
//Gememma		Exosphere
//Gannovar		Exosphere
//Lowel			Exosphere
//Mandrake		Atmo, Exo
//Rutherford	Atmo, Exo

SPACEDUST_RESOURCE
{
	resourceName = LqdHydrogen
	body = Kaywell

	RESOURCEBAND
	{
	name = KaywellExo

	// Maximum and minimum abundances (variation is by game seed)
	// In t/m^3
	 minAbundance = 0.000000000000000000001105
	maxAbundance = 0.000000000000000000003005

	title = #LOC_SpaceDust_Band_Exosphere


	// Discoverability Data
	// --------------
	alwaysDiscovered = false
	alwaysIdentified = false

	// Scale abundances by air density
	useAirDensity = False
	// Distribution model to use, can be Uniform or Spherical
	distributionType = Spherical

	// These parameters are specific to the Spherical model
	// -------
	// Altitudes are in km from sea level
	// Maximum altitude for a distribution
	altUpperBound = 3000000000
	// minimum altitude for a distribution
	altLowerBound = 0
	// peak of a distribution
	altPeak = 300000
	// maximum variation of these altitudes (variation is by game seed)
	altVariability = 0
	// Falloff for altitude, can be Linear or None
	altFalloffType = Linear

	// lats in degrees
	// Maximum latitude for a distribution
	latUpperBound = 90
	// Minimum latitude for a distribution
	latLowerBound = -90
	// peak of a distribution
	latPeak = 0
	// maximum variation of these latitudes (variation is by game seed)
	latVariability = 0
	// Falloff for latitude, can be Linear or None
	latFalloffType = None
	}
}
SPACEDUST_RESOURCE
{
	resourceName = LqdHydrogen
	body = Kaywell

	RESOURCEBAND
	{
	name = LimnellExo

	// Maximum and minimum abundances (variation is by game seed)
	// In t/m^3
	 minAbundance = 0.000000000000000000001105
	maxAbundance = 0.000000000000000000003005

	title = #LOC_SpaceDust_Band_Exosphere


	// Discoverability Data
	// --------------
	alwaysDiscovered = false
	alwaysIdentified = false

	// Scale abundances by air density
	useAirDensity = False
	// Distribution model to use, can be Uniform or Spherical
	distributionType = Spherical

	// These parameters are specific to the Spherical model
	// -------
	// Altitudes are in km from sea level
	// Maximum altitude for a distribution
	altUpperBound = 1000000000
	// minimum altitude for a distribution
	altLowerBound = 0
	// peak of a distribution
	altPeak = 100000
	// maximum variation of these altitudes (variation is by game seed)
	altVariability = 0
	// Falloff for altitude, can be Linear or None
	altFalloffType = Linear

	// lats in degrees
	// Maximum latitude for a distribution
	latUpperBound = 90
	// Minimum latitude for a distribution
	latLowerBound = -90
	// peak of a distribution
	latPeak = 0
	// maximum variation of these latitudes (variation is by game seed)
	latVariability = 0
	// Falloff for latitude, can be Linear or None
	latFalloffType = None
	}
}
SPACEDUST_RESOURCE
{
	resourceName = LqdHydrogen
	body = Gememma

	RESOURCEBAND
	{
	name = GememmaExo

	// Maximum and minimum abundances (variation is by game seed)
	// In t/m^3
	 minAbundance = 0.000000000000000000001105
	maxAbundance = 0.000000000000000000003005

	title = #LOC_SpaceDust_Band_Exosphere


	// Discoverability Data
	// --------------
	alwaysDiscovered = false
	alwaysIdentified = false

	// Scale abundances by air density
	useAirDensity = False
	// Distribution model to use, can be Uniform or Spherical
	distributionType = Spherical

	// These parameters are specific to the Spherical model
	// -------
	// Altitudes are in km from sea level
	// Maximum altitude for a distribution
	altUpperBound = 300000000
	// minimum altitude for a distribution
	altLowerBound = 0
	// peak of a distribution
	altPeak = 30000
	// maximum variation of these altitudes (variation is by game seed)
	altVariability = 0
	// Falloff for altitude, can be Linear or None
	altFalloffType = Linear

	// lats in degrees
	// Maximum latitude for a distribution
	latUpperBound = 90
	// Minimum latitude for a distribution
	latLowerBound = -90
	// peak of a distribution
	latPeak = 0
	// maximum variation of these latitudes (variation is by game seed)
	latVariability = 0
	// Falloff for latitude, can be Linear or None
	latFalloffType = None
	}
}

//Gas Giant Exospheres/Atmospheres

SPACEDUST_RESOURCE
{
  resourceName = LqdHydrogen
  body = Shol


  RESOURCEBAND
  {
    name = SholAtmo
    title = #LOC_SpaceDust_Band_Atmosphere

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.0017
    maxAbundance = 0.0019

    // Discoverability Data
    // --------------
    alwaysDiscovered = true
    alwaysIdentified = false

    // The lower this number, the longer the band will take to discover with a Remote telescope
    remoteDiscoveryScale = 1.0

    
    // Scale abundances by air density
    useAirDensity = True
    // Map air density to scaling factors
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 200000
    // minimum altitude for a distribution
    altLowerBound = 0
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }


  RESOURCEBAND
  {
    name = SholExo
    title = #LOC_SpaceDust_Band_Exosphere

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
     minAbundance = 0.00000000000000000001105
    maxAbundance = 0.00000000000000000002005


    
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 2000000000
    // minimum altitude for a distribution
    altLowerBound = 0
    // peak of a distribution
    altPeak = 200000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

SPACEDUST_RESOURCE
{
  resourceName = LqdHydrogen
  body = Tyepolbynar


  RESOURCEBAND
  {
    name = TyepolbynarAtmo
    title = #LOC_SpaceDust_Band_Atmosphere

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.0017
    maxAbundance = 0.0019

    // Discoverability Data
    // --------------
    alwaysDiscovered = true
    alwaysIdentified = false

    // The lower this number, the longer the band will take to discover with a Remote telescope
    remoteDiscoveryScale = 1.0

    
    // Scale abundances by air density
    useAirDensity = True
    // Map air density to scaling factors
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 200000
    // minimum altitude for a distribution
    altLowerBound = 0
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }


  RESOURCEBAND
  {
    name = TyepolbynarExo
    title = #LOC_SpaceDust_Band_Exosphere

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
     minAbundance = 0.00000000000000000001105
    maxAbundance = 0.00000000000000000002005


    
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 2000000000
    // minimum altitude for a distribution
    altLowerBound = 0
    // peak of a distribution
    altPeak = 200000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

SPACEDUST_RESOURCE
{
  resourceName = LqdHydrogen
  body = Reander


  RESOURCEBAND
  {
    name = ReanderAtmo
    title = #LOC_SpaceDust_Band_Atmosphere

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.0017
    maxAbundance = 0.0019

    // Discoverability Data
    // --------------
    alwaysDiscovered = true
    alwaysIdentified = false

    // The lower this number, the longer the band will take to discover with a Remote telescope
    remoteDiscoveryScale = 1.0

    
    // Scale abundances by air density
    useAirDensity = True
    // Map air density to scaling factors
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 200000
    // minimum altitude for a distribution
    altLowerBound = 0
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }


  RESOURCEBAND
  {
    name = ReanderExo
    title = #LOC_SpaceDust_Band_Exosphere

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
     minAbundance = 0.00000000000000000001105
    maxAbundance = 0.00000000000000000002005


    
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 2000000000
    // minimum altitude for a distribution
    altLowerBound = 0
    // peak of a distribution
    altPeak = 200000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

SPACEDUST_RESOURCE
{
  resourceName = LqdHydrogen
  body = Mandrake


  RESOURCEBAND
  {
    name = MandrakeAtmo
    title = #LOC_SpaceDust_Band_Atmosphere

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.0017
    maxAbundance = 0.0019

    // Discoverability Data
    // --------------
    alwaysDiscovered = true
    alwaysIdentified = false

    // The lower this number, the longer the band will take to discover with a Remote telescope
    remoteDiscoveryScale = 1.0

    
    // Scale abundances by air density
    useAirDensity = True
    // Map air density to scaling factors
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 200000
    // minimum altitude for a distribution
    altLowerBound = 0
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }


  RESOURCEBAND
  {
    name = MandrakeExo
    title = #LOC_SpaceDust_Band_Exosphere

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
     minAbundance = 0.00000000000000000001105
    maxAbundance = 0.00000000000000000002005


    
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 2000000000
    // minimum altitude for a distribution
    altLowerBound = 0
    // peak of a distribution
    altPeak = 200000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

SPACEDUST_RESOURCE
{
  resourceName = LqdHydrogen
  body = Rutherford


  RESOURCEBAND
  {
    name = RutherfordAtmo
    title = #LOC_SpaceDust_Band_Atmosphere

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.0017
    maxAbundance = 0.0019

    // Discoverability Data
    // --------------
    alwaysDiscovered = true
    alwaysIdentified = false

    // The lower this number, the longer the band will take to discover with a Remote telescope
    remoteDiscoveryScale = 1.0

    
    // Scale abundances by air density
    useAirDensity = True
    // Map air density to scaling factors
    densityCurve
    {
        key = 0 0
        key = 1 1
        key = 12 12
    }
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 200000
    // minimum altitude for a distribution
    altLowerBound = 0
    // peak of a distribution
    altPeak = 0
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }


  RESOURCEBAND
  {
    name = RutherfordExo
    title = #LOC_SpaceDust_Band_Exosphere

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
     minAbundance = 0.00000000000000000001105
    maxAbundance = 0.00000000000000000002005


    
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 2000000000
    // minimum altitude for a distribution
    altLowerBound = 0
    // peak of a distribution
    altPeak = 200000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 90
    // Minimum latitude for a distribution
    latLowerBound = -90
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

//Imterril & Tannor, Atmosphere Trace & Exosphere

SPACEDUST_RESOURCE
{
	resourceName = LqdHydrogen
	body = Imterril

	RESOURCEBAND
	{
		name = InterrilExo
		title = #LOC_SpaceDust_Band_Exosphere
		// Maximum and minimum abundances (variation is by game seed)
		// In t/m^3
		minAbundance = 0.00000000000000000001105
		maxAbundance = 0.00000000000000000003005


		 // Visuals Data
		// -------------
		countScale = 50
		rotateRate = 1

		// Discoverability Data
		// --------------
		alwaysDiscovered = false
		alwaysIdentified = false

		// Scale abundances by air density
		useAirDensity = False
		// Distribution model to use, can be Uniform or Spherical
		distributionType = Spherical

		// These parameters are specific to the Spherical model
		// -------
		// Altitudes are in km from sea level
		// Maximum altitude for a distribution
		altUpperBound = 2000000
		// minimum altitude for a distribution
		altLowerBound = 0
		// peak of a distribution
		altPeak = 182000	
		// maximum variation of these altitudes (variation is by game seed)
		altVariability = 0
		// Falloff for altitude, can be Linear or None
		altFalloffType = Linear
		

		// lats in degrees
		// Maximum latitude for a distribution
		latUpperBound = 90
		// Minimum latitude for a distribution
		latLowerBound = -90
		// peak of a distribution
		latPeak = 0
		// maximum variation of these latitudes (variation is by game seed)
		latVariability = 0
		// Falloff for latitude, can be Linear or None
		latFalloffType = None
	}
	
	RESOURCEBAND
	{
		name = InterrilAtmoTrace
		title = #LOC_SpaceDust_Band_Exosphere
		// Maximum and minimum abundances (variation is by game seed)
		// In t/m^3
		minAbundance = 0.00000000000000000001105
		maxAbundance = 0.00000000000000000003005


		 // Visuals Data
		// -------------
		countScale = 50
		rotateRate = 1

		// Discoverability Data
		// --------------
		alwaysDiscovered = false
		alwaysIdentified = false

		// Scale abundances by air density
		useAirDensity = False
		// Distribution model to use, can be Uniform or Spherical
		distributionType = Spherical

		useAirDensity = True
		densityCurve
		{
			key = 0 0
			key = 1 1
			key = 12 12
		}
		
		// These parameters are specific to the Spherical model
		// -------
		// Altitudes are in km from sea level
		// Maximum altitude for a distribution
		altUpperBound = 182000	
		// minimum altitude for a distribution
		altLowerBound = 0
		// peak of a distribution
		altPeak = 0
		// maximum variation of these altitudes (variation is by game seed)
		altVariability = 0
		// Falloff for altitude, can be Linear or None
		altFalloffType = Linear

		// lats in degrees
		// Maximum latitude for a distribution
		latUpperBound = 90
		// Minimum latitude for a distribution
		latLowerBound = -90
		// peak of a distribution
		latPeak = 0
		// maximum variation of these latitudes (variation is by game seed)
		latVariability = 0
		// Falloff for latitude, can be Linear or None
		latFalloffType = None
	}
}

SPACEDUST_RESOURCE
{
	resourceName = LqdHydrogen
	body = Tannor

	RESOURCEBAND
	{
		name = TannorExo
		title = #LOC_SpaceDust_Band_Exosphere
		// Maximum and minimum abundances (variation is by game seed)
		// In t/m^3
		minAbundance = 0.00000000000000000001105
		maxAbundance = 0.00000000000000000003005


		 // Visuals Data
		// -------------
		countScale = 50
		rotateRate = 1

		// Discoverability Data
		// --------------
		alwaysDiscovered = false
		alwaysIdentified = false

		// Scale abundances by air density
		useAirDensity = False
		// Distribution model to use, can be Uniform or Spherical
		distributionType = Spherical

		// These parameters are specific to the Spherical model
		// -------
		// Altitudes are in km from sea level
		// Maximum altitude for a distribution
		altUpperBound = 2000000
		// minimum altitude for a distribution
		altLowerBound = 0
		// peak of a distribution
		altPeak = 112000		
		// maximum variation of these altitudes (variation is by game seed)
		altVariability = 0
		// Falloff for altitude, can be Linear or None
		altFalloffType = Linear
		

		// lats in degrees
		// Maximum latitude for a distribution
		latUpperBound = 90
		// Minimum latitude for a distribution
		latLowerBound = -90
		// peak of a distribution
		latPeak = 0
		// maximum variation of these latitudes (variation is by game seed)
		latVariability = 0
		// Falloff for latitude, can be Linear or None
		latFalloffType = None
	}
	
	RESOURCEBAND
	{
		name = TannorAtmoTrace
		title = #LOC_SpaceDust_Band_Exosphere
		// Maximum and minimum abundances (variation is by game seed)
		// In t/m^3
		minAbundance = 0.00000000000000000001105
		maxAbundance = 0.00000000000000000003005


		 // Visuals Data
		// -------------
		countScale = 50
		rotateRate = 1

		// Discoverability Data
		// --------------
		alwaysDiscovered = false
		alwaysIdentified = false

		// Scale abundances by air density
		useAirDensity = False
		// Distribution model to use, can be Uniform or Spherical
		distributionType = Spherical

		useAirDensity = True
		densityCurve
		{
			key = 0 0
			key = 1 1
			key = 12 12
		}
		
		// These parameters are specific to the Spherical model
		// -------
		// Altitudes are in km from sea level
		// Maximum altitude for a distribution
		altUpperBound = 112000		
		// minimum altitude for a distribution
		altLowerBound = 0
		// peak of a distribution
		altPeak = 0
		// maximum variation of these altitudes (variation is by game seed)
		altVariability = 0
		// Falloff for altitude, can be Linear or None
		altFalloffType = Linear

		// lats in degrees
		// Maximum latitude for a distribution
		latUpperBound = 90
		// Minimum latitude for a distribution
		latLowerBound = -90
		// peak of a distribution
		latPeak = 0
		// maximum variation of these latitudes (variation is by game seed)
		latVariability = 0
		// Falloff for latitude, can be Linear or None
		latFalloffType = None
	}
}